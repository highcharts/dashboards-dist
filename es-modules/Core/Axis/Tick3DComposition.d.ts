import type Tick from './Tick.js';
/**
 * @private
 */
declare function compose(TickClass: typeof Tick): void;
declare const Tick3DAdditions: {
    compose: typeof compose;
};
export default Tick3DAdditions;
